//create nav bar that ALWAYS stays in display 
    //when a category is clicked the user should 
        //use scroll into view OR scroll top
            //possibly mouse coordinate tracker


document.addEventListener("DOMContentLoaded", () =>{

})